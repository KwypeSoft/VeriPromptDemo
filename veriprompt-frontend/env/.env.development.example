# VeriPrompt Development Environment Variables
# Copy this file to `.env.development.local` (git-ignored) to override
# values when running `npm run dev`. You can also copy the contents into
# `.env.local` if you want the same defaults for all modes.

# Smart Contract Address for development/testing
# Replace with your deployed contract address on testnet (e.g., Sepolia)
VITE_CONTRACT_ADDRESS=YOUR_DEV_CONTRACT_ADDRESS

# Backend Cloud Function URL for development
# Replace with your development/staging cloud function URL
VITE_BACKEND_URL=YOUR_DEV_CLOUD_FUNCTION_URL

# Block explorer URL for viewing tokens in development (defaults to Sepolia Etherscan)
VITE_BLOCK_EXPLORER_TOKEN_URL=https://sepolia.etherscan.io/token

# Optional: Add development-specific settings
# VITE_DEBUG_MODE=true
# VITE_LOG_LEVEL=debug
